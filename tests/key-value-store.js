import chai, { assert } from 'chai'
import chaiString from 'chai-string'

chai.use(chaiString)

describe('KeyValueStore', () => {
  it('tests work', () => {
    assert.isTrue(true, 'yup, true is true')
  })
})
